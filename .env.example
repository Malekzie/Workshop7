# docker-compose secrets (dev-only). Copy this file to `.env` and fill in values.
# `.env` is gitignored; never commit real credentials.

POSTGRES_DB=peelin
POSTGRES_USER=baker
POSTGRES_PASSWORD=change-me
